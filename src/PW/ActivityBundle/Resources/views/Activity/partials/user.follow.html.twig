{% extends friend is defined and friend is not empty ? 'PWActivityBundle:Activity:partials/friend.html.twig' : 'PWActivityBundle:Activity:partials/my.html.twig' %}

{% block icon %}
<a class="activityPicture" href="{{ activity.target.user | profile_link }}">
	<img src="{{ activity.target.user.icon | version('medium') }}" alt="{{ activity.target.user.name }}">
</a>
{% endblock %}

{% block message %}
is now following
<a href="{{ activity.target.user | profile_link }}">{{ activity.target.user.name }}</a>
{% endblock %}
